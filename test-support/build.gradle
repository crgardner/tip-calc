jar {
    inputs.property("moduleName", "com.crg.test.support")
    manifest {
        attributes('Automatic-Module-Name': "com.crg.test.support")
    }
}

dependencies {
   compile junitjupiter
   compile mockito
   compile apiguardian
}